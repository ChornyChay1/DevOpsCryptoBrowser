apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: crypto-app
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30924
    protocol: TCP